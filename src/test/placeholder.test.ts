import { describe, it, expect } from 'vitest';

describe('placeholder', () => {
	it('Hugo should write some tests', async () => {
		const procrastinate = true;
		expect(procrastinate).toBeTruthy();
	});
});
